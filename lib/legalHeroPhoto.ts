/**
 * Hero photo lock for the legal page hero. Set via
 * `npm run hero:lock -- <photo-id> legal`.
 * When set, this hero always shows this exact Unsplash photo instead of
 * pulling a new random one on each build.
 */
export const LOCKED_LEGAL_HERO_PHOTO_ID: string | null = "IvpW78GK6iE";
